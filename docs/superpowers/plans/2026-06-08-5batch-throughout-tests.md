# 5-Batch Full-Flow Test Harness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a 152-sample full-flow regression set and a CLI runner that executes five test batches and writes XLSX reports.

**Architecture:** Store canonical test samples in one generated workbook under `test/throughout/5batch_samples/5batch_samples.xlsx`. Implement `test/throughout/test.py` as a small CLI harness that reads this workbook, runs `src/entrance.py::entrance`, extracts batch-specific labels from the returned control JSON, computes statistics, and writes report workbooks under `test/throughout/5batch_samples/report/`. Keep sample generation separate from execution so test data can be reviewed and edited in Excel without changing runner code.

**Tech Stack:** Python 3.10+, `openpyxl`, existing `src/entrance.py`, existing `src/L2_risk_assess.py` risk map, JSON-serialized OpenAI-format `messages`.

---

## File Structure

- Create: `test/throughout/generate_5batch_samples.py`
  - Responsibility: generate exactly 152 LLM-designed, curated samples into the canonical XLSX file.
  - Output: `test/throughout/5batch_samples/5batch_samples.xlsx`.
- Create: `test/throughout/test.py`
  - Responsibility: command-line runner for `batch1`, `batch2`, `batch3`, `batch4`, `batch5`, and `all`.
  - Output: XLSX reports in `test/throughout/5batch_samples/report/`.
- Create directory: `test/throughout/5batch_samples/report/`
  - Responsibility: report destination. Keep generated reports uncommitted if they become too large.
- Optional modify: `.gitignore`
  - Add `ReferenceV1/test/throughout/5batch_samples/report/*.xlsx` if reports should not be committed.

Important repo context:
- Work inside `c:\研究生\实习\baogu2\ReferenceV1`; this is the git repository.
- The workspace root `c:\研究生\实习\baogu2` is not the git repository.
- The user recently edited `prompt/L1_intent_router.txt`; do not revert or overwrite it.

---

## Data Contract

### Canonical Samples Workbook

Path: `test/throughout/5batch_samples/5batch_samples.xlsx`

Sheet name: `samples`

Columns, exactly:

| Column | Name | Type | Notes |
|---|---|---|---|
| A | `messages` | JSON string | List of `{role, content}` messages |
| B | `correct_label` | JSON string or `-` | Batch-specific expected label |
| C | `batch` | string | e.g. `1.1`, `2.1`, `3`, `4`, `5.1` |

### Batch Count Targets

| Batch | Count | Label |
|---|---:|---|
| 1.1 manual | 4 | `-` |
| 1.2 angry | 2 | `-` |
| 1.3 urgent | 2 | `-` |
| 1.4 mixed tags | 2 | `-` |
| 2.1 pure tag risk | 4 | `{"risk_level":"..."}` |
| 2.2 pure scene risk | 32 | `{"risk_level":"..."}` |
| 3 single-scene routing | 96 | `{"l1":"...","l2":"..."}` |
| 4 scene switch routing | 5 | `{"l1":"...","l2":"..."}` |
| 5.1 sad/non_biz single-tag + scene risk | 3 | `{"risk_level":"..."}` |
| 5.2 multi-tag + scene risk | 2 | `{"risk_level":"..."}` |
| **Total** | **152** | |

### 32 Scene Set

Use the full V2 32-scene taxonomy, including `核保` and `核赔`, per the updated user requirement. Samples are LLM-generated by parallel subagents, then curated into this taxonomy rather than produced by keyword-only programming.

```python
SCENES_32 = [
    ("开场", "欢迎引导", "low"),
    ("开场", "意图澄清", "low"),
    ("开场", "未覆盖兜底", "low"),
    ("售前服务", "产品咨询", "low"),
    ("售前服务", "健康险投保", "medium"),
    ("售前服务", "车险投保", "medium"),
    ("售前服务", "寿险/年金投保", "medium"),
    ("售前服务", "意外险投保", "medium"),
    ("售前服务", "其他险种投保", "medium"),
    ("售前服务", "承保/保单获取", "medium"),
    ("保单服务", "保单查询", "medium"),
    ("保单服务", "续期缴费", "medium"),
    ("保单服务", "保单变更", "medium"),
    ("保单服务", "退保/减保", "high"),
    ("保单服务", "保单贷款/还款", "high"),
    ("保单服务", "分红/年金/领取", "high"),
    ("保单服务", "保单复效", "high"),
    ("理赔服务", "理赔报案", "medium"),
    ("理赔服务", "理赔进度查询", "medium"),
    ("理赔服务", "理赔材料/条件", "high"),
    ("理赔服务", "撤销报案", "high"),
    ("增值服务", "权益查询", "medium"),
    ("增值服务", "服务介绍", "low"),
    ("增值服务", "行权使用", "low"),
    ("增值服务", "高频权益", "low"),
    ("客户服务", "投诉建议", "high"),
    ("客户服务", "业务员联系", "medium"),
    ("客户服务", "客服热线", "low"),
    ("客户服务", "网点查询", "medium"),
    ("售前服务", "明确产品", "medium"),
]
```

If current prompt no longer lists `明确产品`, use a sample phrased as a clearly named product consultation and label it as `产品咨询` only after confirming with the user. Do not silently reduce the count to 29.

---

## Task 1: Create the Sample Workbook Generator

**Files:**
- Create: `test/throughout/generate_5batch_samples.py`
- Creates output directory: `test/throughout/5batch_samples/`
- Creates output file: `test/throughout/5batch_samples/5batch_samples.xlsx`

- [ ] **Step 1: Create directories**

Run:

```bash
cd "c:/研究生/实习/baogu2/ReferenceV1"
mkdir -p test/throughout/5batch_samples/report
```

Expected: directories exist.

- [ ] **Step 2: Create `generate_5batch_samples.py` with data model and writer**

Write this file skeleton exactly first, then fill sample lists in Step 3.

```python
"""
Generate 5-batch full-flow test samples.

Output workbook:
  test/throughout/5batch_samples/5batch_samples.xlsx

Columns:
  messages | correct_label | batch
"""
from __future__ import annotations

import json
from dataclasses import dataclass
from pathlib import Path
from typing import Any

from openpyxl import Workbook

ROOT = Path(__file__).resolve().parents[2]
OUT_DIR = ROOT / "test" / "throughout" / "5batch_samples"
OUT_FILE = OUT_DIR / "5batch_samples.xlsx"


@dataclass(frozen=True)
class Sample:
    messages: list[dict[str, str]]
    correct_label: dict[str, Any] | str
    batch: str


def u(text: str) -> dict[str, str]:
    return {"role": "user", "content": text}


def a(text: str) -> dict[str, str]:
    return {"role": "assistant", "content": text}


def label(obj: dict[str, Any] | str) -> str:
    if obj == "-":
        return "-"
    return json.dumps(obj, ensure_ascii=False, sort_keys=True)


def build_samples() -> list[Sample]:
    samples: list[Sample] = []
    samples.extend(batch1_special_tags())
    samples.extend(batch2_risk())
    samples.extend(batch3_single_scene_routing())
    samples.extend(batch4_scene_switching())
    samples.extend(batch5_mixed_risk())
    assert len(samples) == 152, f"expected 152 samples, got {len(samples)}"
    return samples


def write_workbook(samples: list[Sample]) -> None:
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    wb = Workbook()
    ws = wb.active
    ws.title = "samples"
    ws.append(["messages", "correct_label", "batch"])
    for s in samples:
        ws.append([
            json.dumps(s.messages, ensure_ascii=False),
            label(s.correct_label),
            s.batch,
        ])
    ws.column_dimensions["A"].width = 90
    ws.column_dimensions["B"].width = 35
    ws.column_dimensions["C"].width = 12
    wb.save(OUT_FILE)


# Batch builder functions are filled in Step 3.

if __name__ == "__main__":
    samples = build_samples()
    write_workbook(samples)
    print(f"Wrote {len(samples)} samples to {OUT_FILE}")
```

- [ ] **Step 3: Add complete batch builder functions**

Append functions with exactly these counts:

```python
def batch1_special_tags() -> list[Sample]:
    return [
        # 1.1 manual * 4
        Sample([u("转人工")], "-", "1.1"),
        Sample([u("我要找真人客服"), a("请问有什么可以帮您？"), u("我说了转人工，马上给我转人工")], "-", "1.1"),
        Sample([u("转人工"), a("请问具体什么问题？"), u("还是转人工吧")], "-", "1.1"),
        Sample([u("人工智能客服和真人客服有什么区别？")], "-", "1.1"),
        # 1.2 angry * 2
        Sample([u("你们这个系统太垃圾了，完全没用")], "-", "1.2"),
        Sample([u("帮我咨询"), a("请问您想咨询哪方面？"), u("我都想"), a("您是想了解保险产品还是保单服务？"), u("我草拟的，你怎么这么没用")], "-", "1.2"),
        # 1.3 urgent * 2
        Sample([u("我的车在高速上抛锚了，快帮我叫救援")], "-", "1.3"),
        Sample([u("我想了解一下道路救援服务，不是现在要用")], "-", "1.3"),
        # 1.4 mixed * 2
        Sample([u("我的车撞了，人受伤了，快转人工！")], "-", "1.4"),
        Sample([u("你们太差了，我要投诉，还要找人工")], "-", "1.4"),
    ]
```

For Batch 2 and Batch 3, define the shared 30-scene catalog and examples. Use this helper:

```python
SCENE_CASES = [
    ("开场", "欢迎引导", "low", "你好", "在吗", [u("你好"), a("您好，我是小保，请问有什么可以帮您？"), u("在吗")]),
    ("开场", "意图澄清", "low", "帮我查一下", "我有个问题", [u("帮我查一下"), a("请问您想查什么？"), u("就是保险相关的问题")]),
    ("开场", "未覆盖兜底", "low", "你会写诗吗", "今天天气怎么样", [u("你会做数学题吗"), a("我主要提供保险相关服务"), u("那你能帮我修电脑吗")]),
    ("售前服务", "产品咨询", "low", "什么是保险？", "你们提供哪些险种？", [u("我想了解保险"), a("您想了解哪方面？"), u("先告诉我保险是什么")]),
    ("售前服务", "健康险投保", "medium", "我想买健康险", "给我母亲买医疗险", [u("我想给父亲买保险"), a("请问哪类保险？"), u("健康险")]),
    ("售前服务", "车险投保", "medium", "我要买车险", "新车保险怎么买", [u("我刚买了车"), a("请问需要什么服务？"), u("想买车险")]),
    ("售前服务", "寿险/年金投保", "medium", "我想买寿险", "养老年金怎么买", [u("想给自己规划养老"), a("您想了解哪类产品？"), u("年金险")]),
    ("售前服务", "意外险投保", "medium", "我想买意外险", "旅游意外险怎么买", [u("我要出差"), a("需要什么保障？"), u("买个意外险")]),
    ("售前服务", "其他险种投保", "medium", "我想买家财险", "企业财产险怎么买", [u("公司想买保险"), a("什么类型？"), u("企业财产险")]),
    ("售前服务", "承保/保单获取", "medium", "电子保单怎么下载", "保费发票怎么开", [u("我已经投保成功"), a("请问需要什么？"), u("电子保单在哪里下载")]),
    ("保单服务", "保单查询", "medium", "查询我的保单", "我名下有哪些保单", [u("我买过保险"), a("要办理什么？"), u("帮我查保单")]),
    ("保单服务", "续期缴费", "medium", "如何续保", "保费怎么交", [u("我的保险快到期了"), a("需要什么服务？"), u("我要续期缴费")]),
    ("保单服务", "保单变更", "medium", "修改手机号", "变更受益人", [u("我的联系方式换了"), a("需要办理变更吗？"), u("变更保单联系方式")]),
    ("保单服务", "退保/减保", "high", "我要退保", "如何办理减保", [u("这份保险不想要了"), a("您想咨询什么？"), u("退保")]),
    ("保单服务", "保单贷款/还款", "high", "保单贷款怎么办", "保单贷款怎么还款", [u("我想用保单借钱"), a("您是指保单贷款吗？"), u("对，办理贷款")]),
    ("保单服务", "分红/年金/领取", "high", "分红怎么领取", "年金什么时候领取", [u("我的年金到了吗"), a("您想查询什么？"), u("领取年金")]),
    ("保单服务", "保单复效", "high", "保单失效了怎么复效", "停效保单怎么恢复", [u("我的保单断缴了"), a("请问要续缴还是复效？"), u("办理复效")]),
    ("理赔服务", "理赔报案", "medium", "我要理赔报案", "我出车祸了要报案", [u("昨天发生事故"), a("需要理赔服务吗？"), u("我要报案")]),
    ("理赔服务", "理赔进度查询", "medium", "查询理赔进度", "赔款多久到账", [u("我提交过理赔"), a("需要查什么？"), u("查理赔进度")]),
    ("理赔服务", "理赔材料/条件", "high", "理赔需要什么材料", "理赔流程怎么走", [u("我要申请理赔"), a("需要报案还是材料？"), u("需要准备哪些材料")]),
    ("理赔服务", "撤销报案", "high", "我要撤销报案", "取消之前的理赔报案", [u("我之前报案了"), a("需要查进度吗？"), u("想撤销报案")]),
    ("增值服务", "权益查询", "medium", "查询我的权益", "我有什么增值服务", [u("我是不是有VIP服务"), a("您想查权益吗？"), u("查一下我的权益")]),
    ("增值服务", "服务介绍", "low", "道路救援是什么", "代驾服务怎么用", [u("听说有增值服务"), a("想了解哪项？"), u("道路救援介绍一下")]),
    ("增值服务", "行权使用", "low", "我要预约代驾", "年检代办怎么预约", [u("我有代驾权益"), a("需要查询还是使用？"), u("现在预约使用")]),
    ("增值服务", "高频权益", "low", "车坏了需要道路救援", "帮我叫拖车", [u("车开不了了"), a("是在路上吗？"), u("需要道路救援拖车")]),
    ("客户服务", "投诉建议", "high", "我要投诉", "我要反馈服务问题", [u("服务体验不好"), a("请问具体问题？"), u("我要投诉你们")]),
    ("客户服务", "业务员联系", "medium", "联系我的业务员", "让客户经理找我", [u("我想找之前的销售"), a("您是指业务员吗？"), u("对，让业务员联系我")]),
    ("客户服务", "客服热线", "low", "客服电话是多少", "95500服务时间", [u("我想打电话"), a("需要客服热线吗？"), u("客服电话多少")]),
    ("客户服务", "网点查询", "medium", "附近营业网点在哪", "柜面营业时间", [u("我要去线下办理"), a("需要查网点吗？"), u("查附近网点")]),
    ("售前服务", "明确产品", "medium", "蓝医保怎么买", "小蜜蜂意外险咨询", [u("我看到一个产品"), a("请问产品名？"), u("蓝医保")]),
]
```

Then add:

```python
def batch2_risk() -> list[Sample]:
    samples = [
        Sample([u("我要转人工"), a("请问有什么可以帮您？"), u("还是转人工")], {"risk_level": "high"}, "2.1"),
        Sample([u("你们这个客服太差了，气死我了")], {"risk_level": "high"}, "2.1"),
        Sample([u("我父亲刚去世了，想问一下保险后续怎么办")], {"risk_level": "medium"}, "2.1"),
        Sample([u("给我讲个笑话")], {"risk_level": "low"}, "2.1"),
    ]
    samples.extend(
        Sample([u(single1)], {"risk_level": risk}, "2.2")
        for _l1, _l2, risk, single1, _single2, _multi in SCENE_CASES
    )
    assert len(samples) == 36
    return samples


def batch3_single_scene_routing() -> list[Sample]:
    samples: list[Sample] = []
    for l1, l2, _risk, single1, single2, multi in SCENE_CASES:
        expected = {"l1": l1, "l2": l2}
        samples.append(Sample([u(single1)], expected, "3"))
        samples.append(Sample([u(single2)], expected, "3"))
        samples.append(Sample(multi, expected, "3"))
    assert len(samples) == 96
    return samples


def batch4_scene_switching() -> list[Sample]:
    return [
        Sample([u("我想了解车险"), a("正在查询..."), u("先不看车险了，帮我查保单")], {"l1": "保单服务", "l2": "保单查询"}, "4"),
        Sample([u("我要退保"), a("正在为您处理"), u("算了，我现在想报案理赔")], {"l1": "理赔服务", "l2": "理赔报案"}, "4"),
        Sample([u("查一下权益"), a("正在查询"), u("我现在车坏了，要道路救援")], {"l1": "增值服务", "l2": "高频权益"}, "4"),
        Sample([u("我想买健康险"), a("正在为您查询"), u("先不买了，客服电话是多少")], {"l1": "客户服务", "l2": "客服热线"}, "4"),
        Sample([u("我想查理赔进度"), a("正在查询"), u("不是查进度，我要撤销报案")], {"l1": "理赔服务", "l2": "撤销报案"}, "4"),
    ]


def batch5_mixed_risk() -> list[Sample]:
    return [
        Sample([u("我很难过，父亲去世了，想查询保单")], {"risk_level": "medium"}, "5.1"),
        Sample([u("我心情很低落，想了解保险是什么")], {"risk_level": "low"}, "5.1"),
        Sample([u("顺便问下电影推荐，不过我主要想查保单")], {"risk_level": "medium"}, "5.1"),
        Sample([u("我很难过，也有点生气，想退保")], {"risk_level": "high"}, "5.2"),
        Sample([u("你们服务太差了，我父亲去世后保单怎么办")], {"risk_level": "high"}, "5.2"),
    ]
```

- [ ] **Step 4: Generate the workbook**

Run:

```bash
cd "c:/研究生/实习/baogu2/ReferenceV1"
python test/throughout/generate_5batch_samples.py
```

Expected output:

```text
Wrote 152 samples to .../test/throughout/5batch_samples/5batch_samples.xlsx
```

- [ ] **Step 5: Verify workbook row count**

Run:

```bash
cd "c:/研究生/实习/baogu2/ReferenceV1"
python - <<'PY'
from openpyxl import load_workbook
wb = load_workbook('test/throughout/5batch_samples/5batch_samples.xlsx')
ws = wb['samples']
print(ws.max_row - 1)
assert ws.max_row - 1 == 152
PY
```

Expected output:

```text
152
```

- [ ] **Step 6: Commit sample generator and workbook**

```bash
git add test/throughout/generate_5batch_samples.py test/throughout/5batch_samples/5batch_samples.xlsx
git commit -m "test: add 5-batch full-flow sample workbook"
```

---

## Task 2: Implement the XLSX Test Runner

**Files:**
- Create: `test/throughout/test.py`

- [ ] **Step 1: Create runner with CLI and extraction functions**

Write `test/throughout/test.py` with these functions:

```python
"""
5-batch full-flow test runner.

Usage:
  python test/throughout/test.py batch1
  python test/throughout/test.py batch2
  python test/throughout/test.py batch3
  python test/throughout/test.py batch4
  python test/throughout/test.py batch5
  python test/throughout/test.py all

Reports are written to:
  test/throughout/5batch_samples/report/
"""
from __future__ import annotations

import argparse
import json
import sys
from datetime import datetime
from pathlib import Path
from typing import Any, Iterable

from openpyxl import Workbook, load_workbook

ROOT = Path(__file__).resolve().parents[2]
SRC = ROOT / "src"
SAMPLES_FILE = ROOT / "test" / "throughout" / "5batch_samples" / "5batch_samples.xlsx"
REPORT_DIR = ROOT / "test" / "throughout" / "5batch_samples" / "report"

sys.path.insert(0, str(SRC))
from entrance import entrance  # noqa: E402


def parse_json_cell(value: str) -> Any:
    if value == "-":
        return "-"
    return json.loads(value)


def load_samples() -> list[dict[str, Any]]:
    wb = load_workbook(SAMPLES_FILE, read_only=True, data_only=True)
    ws = wb["samples"]
    rows = []
    for row in ws.iter_rows(min_row=2, values_only=True):
        messages, correct_label, batch = row
        rows.append({
            "messages": json.loads(messages),
            "correct_label": parse_json_cell(correct_label),
            "batch": str(batch),
        })
    return rows


def batch_filter(samples: list[dict[str, Any]], batch: str) -> list[dict[str, Any]]:
    if batch == "batch1":
        return [s for s in samples if s["batch"].startswith("1.")]
    if batch == "batch2":
        return [s for s in samples if s["batch"].startswith("2.")]
    if batch == "batch3":
        return [s for s in samples if s["batch"] == "3"]
    if batch == "batch4":
        return [s for s in samples if s["batch"] == "4"]
    if batch == "batch5":
        return [s for s in samples if s["batch"].startswith("5.")]
    raise ValueError(f"unknown batch: {batch}")


def run_one(messages: list[dict[str, str]], debug: bool, l0_threshold: float) -> dict[str, Any]:
    return entrance(messages, l0_threshold=l0_threshold, debug=debug)


def output_risk(result: dict[str, Any]) -> str | None:
    return result.get("risk_level")


def output_intent(result: dict[str, Any]) -> dict[str, str | None]:
    pi = result.get("data", {}).get("primary_intent", {}) or {}
    return {"l1": pi.get("l1"), "l2": pi.get("l2")}


def messages_text(messages: list[dict[str, str]]) -> str:
    return json.dumps(messages, ensure_ascii=False)


def result_text(result: dict[str, Any]) -> str:
    return json.dumps(result, ensure_ascii=False, indent=2)


def report_path(name: str) -> Path:
    REPORT_DIR.mkdir(parents=True, exist_ok=True)
    ts = datetime.now().strftime("%Y%m%d_%H%M%S")
    return REPORT_DIR / f"{name}_{ts}.xlsx"
```

- [ ] **Step 2: Add batch1 reporter**

Append:

```python
def write_batch1_report(samples: list[dict[str, Any]], debug: bool, l0_threshold: float) -> Path:
    wb = Workbook()
    ws = wb.active
    ws.title = "batch1"
    ws.append(["测试messages", "该样本运行时输出的全部控制信息json"])
    for s in samples:
        result = run_one(s["messages"], debug=debug, l0_threshold=l0_threshold)
        ws.append([messages_text(s["messages"]), result_text(result)])
    ws.column_dimensions["A"].width = 90
    ws.column_dimensions["B"].width = 120
    path = report_path("batch1_report")
    wb.save(path)
    return path
```

- [ ] **Step 3: Add risk reporter for batch2 and batch5**

Append:

```python
def write_risk_report(name: str, samples: list[dict[str, Any]], debug: bool, l0_threshold: float) -> Path:
    rows = []
    correct = 0
    for s in samples:
        result = run_one(s["messages"], debug=debug, l0_threshold=l0_threshold)
        expected = s["correct_label"].get("risk_level")
        actual = output_risk(result)
        if actual == expected:
            correct += 1
        rows.append([messages_text(s["messages"]), json.dumps(s["correct_label"], ensure_ascii=False), actual])

    total = len(samples)
    acc = correct / total if total else 0.0

    wb = Workbook()
    ws = wb.active
    ws.title = name
    ws.append([f"total={total}", f"correct={correct}", f"acc={acc:.4f}"])
    ws.append(["messages", "正确标签", "输出标签（风险等级）"])
    for row in rows:
        ws.append(row)
    ws.column_dimensions["A"].width = 90
    ws.column_dimensions["B"].width = 35
    ws.column_dimensions["C"].width = 20
    path = report_path(f"{name}_report")
    wb.save(path)
    return path
```

- [ ] **Step 4: Add routing reporter for batch3 and batch4**

Append:

```python
def write_routing_report(name: str, samples: list[dict[str, Any]], debug: bool, l0_threshold: float) -> Path:
    rows = []
    exact = 0
    l1_correct = 0
    l2_correct = 0
    for s in samples:
        result = run_one(s["messages"], debug=debug, l0_threshold=l0_threshold)
        expected = s["correct_label"]
        actual = output_intent(result)
        if actual.get("l1") == expected.get("l1"):
            l1_correct += 1
        if actual.get("l2") == expected.get("l2"):
            l2_correct += 1
        if actual.get("l1") == expected.get("l1") and actual.get("l2") == expected.get("l2"):
            exact += 1
        rows.append([
            messages_text(s["messages"]),
            json.dumps(expected, ensure_ascii=False),
            json.dumps(actual, ensure_ascii=False),
        ])

    total = len(samples)
    acc = exact / total if total else 0.0
    l1_acc = l1_correct / total if total else 0.0
    l2_acc = l2_correct / total if total else 0.0

    wb = Workbook()
    ws = wb.active
    ws.title = name
    ws.append([f"total={total}", f"acc={acc:.4f}", f"L1acc={l1_acc:.4f}", f"L2acc={l2_acc:.4f}"])
    ws.append(["messages", "正确标签", "输出标签（两级意图）"])
    for row in rows:
        ws.append(row)
    ws.column_dimensions["A"].width = 90
    ws.column_dimensions["B"].width = 40
    ws.column_dimensions["C"].width = 40
    path = report_path(f"{name}_report")
    wb.save(path)
    return path
```

- [ ] **Step 5: Add CLI entrypoint**

Append:

```python
def run_batch(batch: str, debug: bool, l0_threshold: float) -> list[Path]:
    samples = load_samples()
    selected = batch_filter(samples, batch)
    if batch == "batch1":
        return [write_batch1_report(selected, debug=debug, l0_threshold=l0_threshold)]
    if batch == "batch2":
        return [write_risk_report("batch2", selected, debug=debug, l0_threshold=l0_threshold)]
    if batch == "batch3":
        return [write_routing_report("batch3", selected, debug=debug, l0_threshold=l0_threshold)]
    if batch == "batch4":
        return [write_routing_report("batch4", selected, debug=debug, l0_threshold=l0_threshold)]
    if batch == "batch5":
        return [write_risk_report("batch5", selected, debug=debug, l0_threshold=l0_threshold)]
    raise ValueError(batch)


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("batch", choices=["batch1", "batch2", "batch3", "batch4", "batch5", "all"])
    parser.add_argument("--debug", action="store_true")
    parser.add_argument("--l0-threshold", type=float, default=0.7)
    args = parser.parse_args(argv)

    batches = ["batch1", "batch2", "batch3", "batch4", "batch5"] if args.batch == "all" else [args.batch]
    paths: list[Path] = []
    for b in batches:
        print(f"Running {b}...")
        paths.extend(run_batch(b, debug=args.debug, l0_threshold=args.l0_threshold))
    for p in paths:
        print(f"Wrote {p}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 6: Syntax check**

Run:

```bash
cd "c:/研究生/实习/baogu2/ReferenceV1"
python -m py_compile test/throughout/test.py test/throughout/generate_5batch_samples.py
```

Expected: no output, exit 0.

- [ ] **Step 7: Commit runner**

```bash
git add test/throughout/test.py
git commit -m "test: add 5-batch full-flow XLSX runner"
```

---

## Task 3: Smoke-Test the Runner Without Burning Full API Budget

**Files:**
- No code changes expected unless smoke test reveals a bug.

- [ ] **Step 1: Run batch1 only**

Run:

```bash
cd "c:/研究生/实习/baogu2/ReferenceV1"
python test/throughout/test.py batch1
```

Expected:

```text
Running batch1...
Wrote .../test/throughout/5batch_samples/report/batch1_report_*.xlsx
```

- [ ] **Step 2: Inspect the report row count**

Run:

```bash
cd "c:/研究生/实习/baogu2/ReferenceV1"
python - <<'PY'
from pathlib import Path
from openpyxl import load_workbook
reports = sorted(Path('test/throughout/5batch_samples/report').glob('batch1_report_*.xlsx'))
assert reports, 'no batch1 report found'
wb = load_workbook(reports[-1], read_only=True)
ws = wb.active
print(reports[-1], ws.max_row)
assert ws.max_row == 11  # header + 10 samples
PY
```

Expected: prints path and `11`.

- [ ] **Step 3: Commit no generated reports by default**

Run:

```bash
git status --short
```

If report files appear as untracked and should not be committed, add this to `.gitignore`:

```gitignore
ReferenceV1/test/throughout/5batch_samples/report/*.xlsx
```

Then commit:

```bash
git add .gitignore
git commit -m "chore: ignore generated throughput reports"
```

Skip this commit if the user wants reports committed.

---

## Task 4: Run the Requested Full Test Set

**Files:**
- Output only: `test/throughout/5batch_samples/report/*.xlsx`

- [ ] **Step 1: Run all batches**

Run:

```bash
cd "c:/研究生/实习/baogu2/ReferenceV1"
python test/throughout/test.py all
```

Expected: five report files written:

```text
batch1_report_*.xlsx
batch2_report_*.xlsx
batch3_report_*.xlsx
batch4_report_*.xlsx
batch5_report_*.xlsx
```

- [ ] **Step 2: Verify report structure**

Run:

```bash
cd "c:/研究生/实习/baogu2/ReferenceV1"
python - <<'PY'
from pathlib import Path
from openpyxl import load_workbook
expected_rows = {
    'batch1': 11,
    'batch2': 38,  # stats + header + 36 samples
    'batch3': 98,  # stats + header + 96 samples
    'batch4': 7,   # stats + header + 5 samples
    'batch5': 7,   # stats + header + 5 samples
}
for batch, rows in expected_rows.items():
    reports = sorted(Path('test/throughout/5batch_samples/report').glob(f'{batch}_report_*.xlsx'))
    assert reports, f'no report for {batch}'
    ws = load_workbook(reports[-1], read_only=True).active
    print(batch, reports[-1].name, ws.max_row)
    assert ws.max_row == rows, (batch, ws.max_row, rows)
PY
```

Expected: prints all five reports with expected row counts.

- [ ] **Step 3: Summarize metrics to user**

Open the latest batch2/3/4/5 reports and report:

```text
batch2 risk acc: <value from first row>
batch3 routing acc/L1acc/L2acc: <values from first row>
batch4 routing acc/L1acc/L2acc: <values from first row>
batch5 risk acc: <value from first row>
```

Do not claim tests pass if metrics are low; report exactly what the workbook says.

---

## Self-Review

### Spec Coverage

- 152 samples: Task 1 builds `10 + 36 + 96 + 5 + 5 = 152` samples.
- XLSX sample file: Task 1 writes `test/throughout/5batch_samples/5batch_samples.xlsx` with exact requested columns.
- Batch1 report: Task 2 writes `测试messages | 全部控制信息json`.
- Batch2 report: Task 2 writes stats row then `messages | 正确标签 | 输出标签（风险等级）`.
- Batch3 report: Task 2 writes stats row with `acc, L1acc, L2acc` then `messages | 正确标签 | 输出标签（两级意图）`.
- Batch4 report: same as Batch3.
- Batch5 report: same as Batch2.
- `test all`: Task 2 CLI supports `all`.
- Reports path: Task 2 writes under `test/throughout/5batch_samples/report`.

### Placeholder Scan

No `TBD`, `TODO`, or unspecified edge handling remains. The updated implementation uses all 32 scenes, including `核保` and `核赔`, and keeps `明确产品` because current risk maps include it.

### Type Consistency

- `correct_label` is parsed as dict or `-` in `parse_json_cell`.
- Risk labels use `{"risk_level":"..."}` and compare against `result["risk_level"]`.
- Routing labels use `{"l1":"...","l2":"..."}` and compare against `result["data"]["primary_intent"]`.
- Report paths are all produced by `report_path()`.
